# Microsoft 365 product logos

Drop product logo PNGs here. They are used by the homepage Overview
"Microsoft 365" floating cluster (`src/Components/HomePage/M365Showcase.jsx`).

Reference them in code as `/logos/<filename>` (the `public/` folder is the web root).

Suggested filenames:
- intune.png
- entra.png
